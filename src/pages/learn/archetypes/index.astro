---
import LearnLayout from '../../../layouts/LearnLayout.astro';
import { Header, Footer, ArchetypeGallery } from '../../../components/Islands';
---

<LearnLayout
	title="Archetypes | Learn | Agentic Loops"
	breadcrumbs={[{ label: 'Archetypes' }]}
>
	<Header client:load slot="header" />

	<section class="section relative overflow-hidden">
		<div class="container">
			<div class="text-center mb-14">
				<div class="flex items-center justify-center gap-4 mb-6">
					<div class="w-10 h-0.5" style="background: var(--color-accent-primary)"></div>
					<span class="badge">archetypes</span>
					<div class="w-10 h-0.5" style="background: var(--color-accent-primary)"></div>
				</div>

				<h1 class="mb-6">
					Architecture Archetypes
				</h1>

				<p class="hero-tagline max-w-[600px] mx-auto">
					Pre-built capability profiles for common goals. Pick one to see which
					dimensions matter most and get a curated learning path.
				</p>
			</div>

			<ArchetypeGallery client:visible />
		</div>
	</section>

	<Footer client:load slot="footer" />
</LearnLayout>
